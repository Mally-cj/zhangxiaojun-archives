# program

将《张小珺商业访谈录》播客的原始转录稿转化为适合手机/网页阅读的书籍章节 HTML。

## 你的任务

检查 `原文/` 目录：**所有 `.docx` 文件都是待处理的转录稿，`.md` 文件是已处理过的（跳过）。**

对每个待处理的 docx：先转为 md → 再按 Prompt 生成 HTML → 保存 HTML → 更新 index.html → 删除原 docx（md 保留在原文/里作为存档和已处理标记）。

## 工作流程

1. **找到待处理文件**：列出 `原文/` 下所有 `.docx` 文件。`.md` 文件直接跳过。
2. **转为 md**：用 `textutil -convert txt` 将 `.docx` 转为 `.md`，保存到 `原文/` 同目录下（macOS 内置命令，可能需要点一次批准）。文件名 = 原 docx 名去掉 `.docx` + `.md`。
3. **读取 md**：用 Read 工具读取刚生成的 md 文件，获取完整转录文本。
4. **生成 HTML**：按照下面的写作 Prompt 生成书籍章节 HTML。必须参照 `书籍章节/` 中已有的 HTML 文件保持完全一致的 CSS 和页面结构。
5. **保存 HTML**：写入 `书籍章节/`，文件名 = md 文件名去掉 `_原文.md` + `_书籍章节.html`。
6. **更新 index.html**：在章节列表区域添加新章节入口卡片。
7. **删除 docx**：删除原 `.docx` 文件，只保留 `.md`。
8. **Git 提交并推送**：`git add 原文/ 书籍章节/ index.html program.md README.md && git commit -m "add: 第X期 <嘉宾名> <主题关键词>" && git push`。每处理完一篇就提交一次，不要攒到一起。
9. **继续下一篇**：重复步骤 2–8，直到所有 docx 处理完毕。最后报告处理结果。

## 更新 index.html 的规则

在 `index.html` 的 `<div class="section-title">已 收 录 章 节</div>` 下方，参照已有 `.chapter-card` 的格式添加新条目：

- `num` 显示期数和日期（从原文头部提取）
- `h3` 显示章节标题
- `meta` 显示嘉宾和机构
- `keywords` 显示核心关键词

每篇原文处理完成后立即更新一次 index.html，确保随时可以查看进度。

## 写作 Prompt

以下是你加工每一篇转录稿时必须严格遵循的规范。

---

# 角色与使命
你的任务是将《张小珺商业访谈录》的一集长篇深度访谈，转化为一本商业传记书籍中的核心章节。参考顶尖非虚构传记的写作风格（如《史蒂夫·乔布斯传》《激荡三十年》），执行一场"**纸上的深度纪录片记录稿**"：通过文字还原商业社会真实的刀光剑影、创业者背后最真实的思考起伏，以及那些未经修饰、带有颗粒度的第一手洞见。

---

## 核心指导原则：真实性与完整性（最高优先级）
1.  **完整性压倒一切**：保留受访者所有的逻辑推演过程、论点论据及背景细节。严禁任何形式的概括、浓缩或删减。
2.  **专有名词与事实校验**：
    *   身份校验：准确识别受访者姓名，并在全文中统一使用正确的汉字，严禁出现语音转录（ASR）中常见的同音错别字。
    *   术语校准：严格核对本访谈涉及的商业与技术专有名词。包括但不限于：公司名、产品名、行业术语、英文缩写及特定的商业概念，确保拼写及上下文逻辑准确。
    *   逻辑修正：识别并修正语音转录中的同音错别字（如将"投后"误写为"投厚"），但在修正时严禁改变受访者的原话本意。
3.  **回归真实的表达与脉络**：原封不动地保留受访者具有"肌肉记忆"的原始表达（包括特定的比喻、口头禅、非标词汇、自嘲等）。仅去除纯粹的口语填充词（如"那个"、"呃"）。禁止重写或美化受访者的语气。
4.  **拒绝平庸的总结**：禁止写"他们讨论了人工智能的未来"这种总结性语句。必须通过重组对话，让读者看到人物如何一步步推演出结论。
5.  **严禁文学化编造**：严禁编造原稿中不存在的环境描写、人物动作、神态描写及心理状态臆测。所有的张力必须来自于对话本身、思想的交锋和事实的陈述。

---

## 写作结构要求
1.  **每一章的固定开头格式**
    每一章的第一页必须包含以下四个元素，严禁遗漏：
    *   **题目**：具有传记质感的章节大标题。
    *   **核心关键词**：提取本章最核心的1-2个商业/技术关键词。
    *   **播客标题**：注明原始播客的完整题目。
    *   **嘉宾引言（Quote）**：摘录一段嘉宾最具冲击力或最能代表本章精神的原话。
2.  **禁止小标题**
    *   纯长文叙述：章节内部禁止使用任何形式的小标题、副标题或小章节划分。
    *   禁止列表：严禁使用项目符号（Bullet Points）或数字列表。
    *   段落衔接：通过逻辑自然推进，段落之间空一行，体现纪录片的"剪辑感"与连贯性。

---

## 写作流程细则
*   **场景化还原**：必须从访谈中最具张力的时刻、深刻的问题或受访者当时的心境切入，迅速带入纪录片的临场感。
*   **保留互动张力**：完整保留张小珺具有拆解性、追问性甚至攻击性的原始提问，展现真实的商业交锋。
*   **人物刻画与关系**：第一次提到某个名字（投资人、对手、合伙人）时，用1-2句极其传神的话介绍其身份及重要性。通过原语呈现受访者的真实状态（焦虑、狂妄、理性的肌肉记忆）。
*   **专业概念的"耳边批注"**：针对技术术语或行业黑话，在第一次出现时利用嘉宾原话自然揉进叙事。要求短小精悍，像作者在耳边的轻声批注，而非教科书定义。
*   **真实性校验**：严禁删除访谈末尾提到的生活细节（如健康建议、饮食偏好等），这些是体现"人"的真实性的核心。

---

## 最终成品排版规范
1.  **书籍质感排版**：
    *   每页文字必须铺满（约2000-3000字）。
    *   英文/数字使用 **Baskerville** 字体，中文使用衬线宋体（思源宋体）。
    *   模拟象牙色纸张质感（`#fdfaf2`）。
    *   每页底部正中标明页码（如 `- 1 -`）。
2.  **篇幅要求**：总篇幅必须根据原稿深度扩展至20-30页，确保每一页的信息密度。

---

## HTML 模板

输出的 HTML 必须使用以下完整模板结构。参照 `书籍章节/141_Freda的投资札记第2集_书籍章节.html` 的完整 CSS 样式和页面结构。

关键 HTML 要点：
- CSS 全部内联在 `<style>` 标签中，不依赖外部样式表
- 每页用 `<div class="page">` 包裹，底部 `<div class="page-num">- N -</div>`
- 第一页包含章节标题、关键词、播客标题、嘉宾引言（quote）
- 最后一页为版权尾页（min-height:auto），包含播客链接、制作流程、版权声明
- 英文和数字用 `<span class="english">` 或 `<span class="num">` 包裹
- 桌面端页面宽度 680px，移动端自动适配

每篇转录稿的 HTML 输出必须完全自包含，可以直接在浏览器中打开阅读。
